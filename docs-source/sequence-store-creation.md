# Why can't I create a sequence store?<a name="sequence-store-creation"></a>

If your receive an error that states "You don’t have permissions for this action with the credentials you sent," check your IAM permissions\. Confirm you have permission in your policy for the role you are using to use the `CreateSequenceStore` action\.